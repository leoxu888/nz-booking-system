# nz-booking-system 免费部署与找客户指南

> 目标：0 成本把系统跑成一个公开网址（店家能访问、数据不丢），并拿到第一批真实商户。
> 前置：代码已改造成「Postgres 生产 / SQLite 本地」双模式（见 database.py）。本指南假设你已有 GitHub 账号。

---

## 一、100% 免费部署（数据库 + 网站）

### 第 1 步：建一个免费 Postgres 数据库
推荐 **Supabase**（免费额度：2 个项目 / 500MB，足够起步）：
1. 打开 supabase.com 注册 → New Project → 填名字、设一个强数据库密码 → 等 1–2 分钟初始化。
2. 左侧 **Settings → Database**，找到 **Connection string（URI）**，复制那行
   `postgresql://postgres:你的密码@db.xxxx.supabase.co:5432/postgres?sslmode=require`
3. 这串就是你的 `DATABASE_URL`（含 sslmode，Supabase 强制加密，必须带）。

> 备选：Neon（neon.tech，免费 512MB）步骤类似，复制它的连接串即可。
> 免费层有时要求绑卡做"验证"，但**不会扣费**；若你坚持完全不绑卡，DB 也可改用 **Render 的免费 Postgres**（90 天到期，需续）。

### 第 2 步：把代码推到 GitHub
```bash
cd nz-booking-system
git init -q
git add -A
git commit -q -m "init: booking system (postgres-ready)"
# 在 github.com 新建一个空仓库，然后：
git remote add origin git@github.com:你的用户名/nz-booking-system.git
git push -u origin main
```

### 第 3 步：部署网站（免费 Web 服务）
推荐 **Koyeb**（免费层、通常免绑卡）：
1. koyeb.com 注册 → **Create App → 从 GitHub 导入该仓库**。
2. 构建/启动命令填：
   - Build：`pip install -r requirements.txt`
   - Run：`uvicorn main:app --host 0.0.0.0 --port $PORT`
3. 在 **Environment variables** 里加：
   | 变量 | 值 |
   |---|---|
   | `DATABASE_URL` | 第 1 步复制的 Supabase URI |
   | `SECRET_KEY` | 随便一段长随机串（如 `python -c "import secrets;print(secrets.token_hex(32))"`） |
   | `SUPER_ADMIN_PASSWORD` | 强密码 |
   | `SHOP_EMAIL` | 你的邮箱 |
   | `SHOP_TZ` | `Pacific/Auckland` |
4. Deploy → 它会给你一个 `xxx.koyeb.app` 网址，店家就能访问了。

> 备选：**Render** 免费 Web 服务（免费额度，闲置后首次访问冷启动慢几秒）；**Fly.io** 免费额度（需绑卡验证）。Koyeb 最省事、免绑卡，优先。

### 第 4 步（可选）：买个 .nz 域名
在 ionos / crazy domains / obay 等注册 `你的品牌.co.nz`（约 $30–50/年），把 CNAME/ALIAS 指向平台给的网址。起步阶段用平台自带网址也完全够用。

### 第 5 步：加隐私政策页
在 `static/` 里放一个 `privacy.html`，页脚加链接，留你的邮箱当"隐私官"——上架前补，半天事。这点是真实合规卖点（我们已修掉最危险的 PII 泄露漏洞）。

### 验证数据库连通（部署后可选自检）
本地跑：
```bash
DATABASE_URL="postgresql://...?sslmode=require" python -c "import database; database.IS_POSTGRES=True; database.init_db(); print('Postgres 初始化 OK')"
```
能打印 `Postgres 初始化 OK` 即说明表结构在 Postgres 上建好了。

---

## 二、怎么找客户（你不认识很多人也能做）

核心思路：**你不需要认识很多人，需要的是"主动触达 + 让用过的店替你说话"。** 系统现在逻辑已够硬（不会双卖、隐私不裸奔），所以你只要把"第一家"谈下来，案例就有了。

### 打法 A：Google Maps 冷触达（最靠谱，零人脉）
1. 打开 Google Maps 搜：`hair salon Auckland`、`barber Auckland`、`美甲 奥克兰`、`massage Auckland`、`nail salon Auckland`。
2. 重点搜**华人聚集区**：中区 Dominion Rd / 北岸 Albany–Takapuna / 东区 Howick–Botany / 西区 Henderson。
3. 点进店铺拿电话，直接打电话或 walk-in：
   > "你好，我是做在线预约系统的，专门帮小店接网上预约、自动发提醒、不会像微信接龙那样漏单或重复接两个人。第一个月免费给你用，你只用收客人，不满意随时停。方便的话我帮你 10 分钟装好？"
4. 一次下午打 20 家，按 5% 转化率也有 1 家愿意试。

### 打法 B：微信 / 小红书内容获客（躺赢型）
- 发图文/短视频："奥克兰开店，还在用微信接龙接预约？3 个会漏单的坑 + 我帮 XX 店装的免费系统实拍"。
- 带店铺后台截图、顾客收到确认短信的截图，真实感最强。
- 评论区/私信导流到你的微信。

### 打法 C：异业转介（杠杆最大）
找这些人对接，谈**每成交一家返 1 个月订阅（约 $15–39）**：
- 华人会计 / 记账公司（手上有大量新注册店铺客户）
- 移民顾问 / 留学中介
- 华人地产中介（新租客开店）
- 开店批文/装修顾问

### 打法 D：地推 + 商会（补人脉）
- 打印一页中文单页，去华人商圈理发店、美容院挨家发，留微信二维码。
- 参加新西兰中国商会、各地同乡会活动（多免费），认识店主本人。

### 起步 30 天目标（可执行）
| 周次 | 动作 | 目标 |
|---|---|---|
| 第 1 周 | 部署上线 + 找 1 家熟人/Google 搜来的店免费试用 | 1 家上线 |
| 第 2–3 周 | 用这家店的截图/推荐语，再谈 3–5 家 | 累计 3–5 家试用 |
| 第 4 周 | 推动 1 家转付费（$15/月 Starter），把案例发小红书/社群 | 第 1 笔收入 + 案例素材 |

> 关键钩子：**"前 X 家免费试用，条件是允许我把它当案例。"** 用案例滚雪球，比你挨家解释强 10 倍。

---

## 三、上线前自检清单
- [ ] `DATABASE_URL` 已设为 Supabase/Neon 连接串（带 sslmode）
- [ ] `SECRET_KEY` / `SUPER_ADMIN_PASSWORD` 已设（非默认值）
- [ ] 网站能公开访问，demo 店铺能预约、老板后台能看到
- [ ] 隐私政策页 + 你的邮箱已挂上
- [ ] 至少 1 家真实店铺在试用
